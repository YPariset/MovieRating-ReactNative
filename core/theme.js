import { DefaultTheme } from 'react-native-paper'

export const theme = {
  ...DefaultTheme,
  colors: {
    ...DefaultTheme.colors,
    text: '#FFFFFF',
    primary: '#E41F24',
    secondary: '#000000',
    error: '#E41F24',
  },
}
